# URL of the backend API (leave empty to use Vercel rewrites proxy)
VITE_API_URL=

# Dashboard access key — must match DASHBOARD_API_KEY on the backend
# Leave empty to disable auth (not recommended for production)
VITE_API_KEY=your-secret-access-key
